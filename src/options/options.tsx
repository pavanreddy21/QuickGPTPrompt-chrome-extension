import React from "react";
import Tabs from "../tabs/tabs";

const Options = () => {
    return (
        <div>
            <h1 className="text-4xl text-green-500">Hello Options</h1>

            {/* <Tabs/> */}
        </div>
    )
};

export default Options;